% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kdf_hkdf.R
\name{rkdf_kdf_hkdf}
\alias{rkdf_kdf_hkdf}
\title{HKDF - HMAC-based Extract-and-Expand Key Derivation Function (RFC-5869)}
\usage{
rkdf_kdf_hkdf(salt, ikm, info, len, hash)
}
\arguments{
\item{salt}{(character or raw) Optional salt value. If \code{NULL} or empty, a string of zeros
equal to the PRF's hash output length is used (per RFC 5869).}

\item{ikm}{(character or raw) Input keying material. Must be provided.}

\item{info}{(character or raw) Optional context/application-specific info string.}

\item{len}{(integer) Length in bytes of output keying material. Must be positive and
not greater than \code{255 * HashLen}, where \code{HashLen} is the output length (in bytes) of the Hash.}

\item{hash}{A PRF implementation (HMAC) to use. Accepts character name matching the algorithm
name or OID available in \code{algorithm_list}.}
}
\value{
A list with classes \code{hkdf_result} and \code{rkdf_result} containing:
\itemize{
\item \code{key}: raw vector with \code{len} bytes OKM,
\item \code{algorithm}: the string \code{"hkdf"},
\item \code{parameters}: an object of class \code{hkdf_parameters} with fields \code{salt}, \code{len}, \code{info} and
\code{hash}
\item \code{timestamp}: UTC timestamp string.
}
}
\description{
Derive a key using the HMAC-based Extract-and-Expand Key Derivation Function (HKDF).
This function performs \code{Extract} (HMAC(salt, IKM)) followed by \code{Expand} to produce
\code{len} bytes of output key material (OKM).
}
\details{
This implementation follows RFC 5869. Important notes:
\itemize{
\item If \code{salt} is omitted, it is replaced with \code{0x00} repeated \code{HashLen} times (per RFC).
\item The function enforces the RFC limit: \code{len <= 255 * HashLen}.
}
}
\examples{
# using a named PRF (friendly name must be registered in algorithm_list)
res <- rkdf_kdf_hkdf(NULL, "input-key", "context", 32, "hmac_sha256")
cat(res$key)

}
